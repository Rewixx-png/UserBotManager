# Менеджер User-Аккаунтов Telegram

Это многофункциональный Telegram-бот, написанный на **Aiogram 3.x** и **Telethon**, для управления вашими пользовательскими аккаунтами (userbots). Проект полностью упакован в Docker для максимальной простоты развертывания и переносимости.

## 🚀 Ключевые возможности

*   **Добавление и управление аккаунтами** через удобный интерфейс.
*   **Проверка валидности сессии** с отображением статуса (✅/❌).
*   **Просмотр подробной информации** об аккаунте (ID, имя, Premium статус и т.д.).
*   **Быстрый доступ к кодам авторизации** из сервисного чата Telegram (777000).
*   **Экспорт `.session` файла** для использования в других проектах на Telethon.
*   **Безопасность**: Доступ к боту ограничен только владельцем, а все секретные данные передаются через переменные окружения.

## 🛠️ Стек технологий

*   **Telegram Бот**: [Aiogram 3.x](https://github.com/aiogram/aiogram)
*   **Userbot (Клиент API)**: [Telethon](https://github.com/LonamiWebs/Telethon)
*   **База данных**: SQLite
*   **Развертывание**: [Docker](https://www.docker.com/) и **Docker Compose**

---

## 🚀 Установка и Запуск

Единственный способ запуска этого бота — через Docker Compose. Это обеспечивает изоляцию, безопасность и простоту управления.

### Требования

*   [Docker](https://docs.docker.com/get-docker/)
*   [Docker Compose](https://docs.docker.com/compose/install/)

### Шаг 1: Клонирование репозитория

Откройте терминал на вашем сервере или локальной машине и выполните команду:
```bash
git clone <URL вашего репозитория>
cd <папка репозитория>
```

### Шаг 2: Создание файла конфигурации `.env`

Создайте файл с именем `.env` в корневой папке проекта. Этот файл будет хранить ваши секретные данные. **Он уже добавлен в `.gitignore` и никогда не должен попадать в репозиторий!**

Заполните `.env` вашими данными:
```env
# Токен вашего управляющего бота, полученный у @BotFather
BOT_TOKEN=123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11

# Ваш числовой ID в Telegram (узнать у @userinfobot)
OWNER_ID=123456789
```

### Шаг 3: Запуск бота

Выполните одну команду, чтобы собрать образ и запустить контейнер в фоновом режиме:
```bash
docker compose up --build -d
```
Бот запущен! Все данные (база данных и сессии) будут храниться в папке `data`, которая автоматически создастся в директории проекта.

---

## 📦 Управление контейнером

*   **Просмотр логов бота в реальном времени:**
    ```bash
    docker compose logs -f
    ```
    *(Нажмите `Ctrl+C`, чтобы выйти из режима просмотра логов)*

*   **Остановка бота и удаление контейнера:**
    ```bash
    docker compose down
    ```

*   **Пересборка и перезапуск (если вы изменили код проекта):**
    ```bash
    docker compose up --build -d
    ```